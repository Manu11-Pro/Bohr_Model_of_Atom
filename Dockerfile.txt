FROM python:3.14-slim

RUN apt-get update && apt-get install -y git && rm -rf /var/lib/apt/lists/*

WORKDIR /app

RUN pip install --no-cache-dir viser trimesh numpy pandas openpyxl

COPY . .

EXPOSE 7860

CMD ["python", "Bohr_Model_of_Atom.py"]